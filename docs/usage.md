# Usage

Build a custom model from its Modelfile:

```bash
ollama create <model-name> -f ./src/<model-name>/Modelfile
```

Run it:

```bash
ollama run <model-name>
```

List installed models, or remove one:

```bash
ollama list
ollama rm <model-name>
```

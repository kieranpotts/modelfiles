#
# <Model name>
#
# Describe the capability this model provides — the kind of work it is
# tuned for. Do NOT define a role or persona; that is supplied by the
# agent harness's system prompt.
#
# See https://docs.ollama.com/modelfile for the full Modelfile reference.
#

# The base model to customize. Reference a model from the Ollama library
# (https://ollama.com/library) or a locally available base model.
FROM <base-model>

# Runtime parameters tuned for this capability. Remove any you do not need,
# and note the rationale for your choices in comments.
# See https://docs.ollama.com/modelfile#valid-parameters-and-values
PARAMETER temperature 0.7
# PARAMETER num_ctx 4096
# PARAMETER top_p 0.9
# PARAMETER stop "<stop-token>"

# Do NOT add a SYSTEM block. The role and persona are defined by the agent
# harness's system prompt, not by the model.
